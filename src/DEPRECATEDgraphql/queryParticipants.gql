query queryParticipants($eventId: ID!) {
  events(eventId: $eventId) {
    id
    participants {
      id
      name
    }
  }
}

query getParticipants($eventId: String!) {
  participants(eventId: $eventId) {
    id
    name
  }
}
