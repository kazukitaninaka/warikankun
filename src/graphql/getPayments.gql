query getPayments($eventId: String!) {
  payments(eventId: $eventId) {
    id
    name
    amount
    createdAt
    whoShouldPay {
      id
      name
    }
    whoPaid {
      id
      name
    }
  }
}
