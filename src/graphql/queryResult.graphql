query QueryResult($eventId: uuid!) {
  QueryResult(eventId: $eventId) {
    id
    sumPrice
    name
    transactions {
      from {
        id
        name
      }
      to {
        id
        name
        amount
      }
    }
  }
}
